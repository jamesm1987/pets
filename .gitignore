###############################################################
# Root-level ignores (apply to entire project)
###############################################################

# OS junk
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/

# Environment variables (global or subfolders)
.env
.env.*.local
*/.env
*/.env.*.local
!.env.example

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun-debug.log*

node_modules/


###############################################################
# Client (Vite + React + TypeScript)
###############################################################

client/node_modules/
client/dist/
client/dist-ssr/
client/build/

# Vite config caches
client/vite.config.js.timestamp*
client/vite.config.ts.timestamp*

client/*.tsbuildinfo

# Optional caches
client/.cache/
client/.eslintcache
client/.stylelintcache
client/.parcel-cache/

# Storybook (if ever added)
client/storybook-static/


###############################################################
# Backend (Node + Express + TypeScript)
###############################################################

backend/node_modules/
backend/dist/

# TS build info
backend/*.tsbuildinfo

# Local SSL certs
backend/certs/
backend/*.pem
backend/*.key

# Coverage
backend/coverage/
backend/*.lcov


###############################################################
# Misc (shared workspace)
###############################################################

# Yarn / npm / pnpm lock backups
*.lock.backup

# Temporary files
*.tmp
*.temp
